<%= render partial: 'sidebar' %>

<div class="span8">
	<h3>New jokes:</h3>
	<% @jokes.each do |j| %>
		<div class="row-fluid">
			<h3><%= link_to j.title, joke_path(j) %></h3>
			<p><%= j.content %></p>
		</div>
		<% if current_user && j.user.id && current_user.id == j.user.id %>
		<div class="btn-group">
			  <%= link_to "Edit", edit_joke_path(j), :class=>'btn btn-info' %>
			  <%= link_to "Delete", j, method: :delete, data: {confirm: 'Are you sure?'}, :class=>'btn btn-danger' %>
		</div>
		<% end %>
		
		<% if current_user %>
		<%= link_to 'Upvote', 
			{:controller =>"jokes", :action => "upvote", :id => j.id}, :class => 'btn', method: :put %>
		<%= link_to 'Downvote', 
			{:controller =>"jokes", :action => "downvote", :id => j.id}, :class => 'btn', method: :put %>
		<% end %>
		<hr />
	<% end %>
</div>